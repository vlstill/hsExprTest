<!doctype html>
<html lang=en>
  <head>
	<title>External evaluator for IS</title>
	<link rel="stylesheet" href="stylesheet.css" type="text/css">
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
	<meta name="Author" content="Vladimír Štill">
	<meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    table, th, td {
      border: 1px solid black;
      border-collapse: collapse;
    }
    td { padding: 0.2em; }
  </style>
  </head>

  <body>
	<h1>Log summary for {{course}}</h1>
  <p>
    Logs by days:<br>
    {% for date in dates %}
      <a href="log;from={{date.stamp}};to={{date.stamp}}">
          {{date.stamp}} ({{date.count}} entries)
      </a><br>
    {% endfor %}
  </p>
  </body>
</html>
<!-- vim: expandtab tabstop=2 ft=html
-->
