language: haskell
ghc:
  - 7.8

env:
    - GHC_PATH=/opt/ghc/7.8.2/bin
    - GHC_PATH="" # default ghc specified above

before_install:
    - ls /opt/ghc/
    - ls /usr/local/ghc/
    - [ -z $GHC_PATH ] || PATH=$GHC_PATH:$PATH
    - ghc --version

install:
    - cabal install --only-dependencies --enable-tests

script:
    - cabal configure
    - cabal build
    - cabal install
    - cabal configure --enable-tests
    - cabal test --show-details=always

notifications:
  email:
    recipients:
      - xstill@fi.muni.cz
    on_success: change # default: change
    on_failure: always # default: always

sudo: false

addons:
  apt:
    sources:
      - hvr-ghc
    packages:
      - socat
      - time
      - ghc-7.10.2
